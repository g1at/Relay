# Relay 2.1.0 更新日志

> 发布日期：2026-08-11  
> 升级范围：2.0.0 → 2.1.0

本次更新重点改善模型控制、上下文感知、附件展示和定时任务稳定性。覆盖安装后，原有历史会话、设置、定时任务、技能、记忆及本地文件不会被删除。

## 面向用户的更新

### 模型与推理强度

- 模型菜单会读取当前账户实际支持的 Claude 模型，并兼容带扩展上下文窗口的模型。
- 新增推理强度调节，可在低、中、高、极高、最大之间切换；当前选择会显示在输入区并随会话保存。
- 模型和推理强度可在空闲会话中直接切换，无需重连 MCP 或重新创建整段对话。
- 重做模型菜单与推理强度滑杆，缩小面板尺寸，加入页面切换和档位靠近动画，并修复滑杆填充缝隙与溢出。

### 上下文占用提示

- 发送按钮旁新增上下文占用百分比，便于在长对话中判断剩余空间。
- 悬停后显示已用 Token、上下文上限、当前模型和占用进度；高占用时自动切换警告颜色。
- 历史对话会保存最近一次上下文占用，切换会话或重启 Relay 后立即显示缓存，再在后台刷新最新数据。

### 附件与图片展示

- PDF、Word、表格、演示文稿、Python、JavaScript、Markdown、压缩包、音视频等文件使用更清晰的类型图标和颜色。
- 输入框中的图片改为紧凑缩略图预览，普通文件继续显示名称、格式与大小。
- 图片和文件的删除按钮统一为尺寸一致的深色圆形按钮。

### 定时任务

- 修复点击“立即运行”时偶发提示“任务不存在”的问题。
- 点击后立即确认任务已进入执行态，不再等待长任务完成才返回结果。
- 任务卡片会显示“运行中”，执行期间禁止重复启动。
- 读取或保存任务数据失败时给出准确提示，不再错误伪装成任务不存在；异常结束后也会可靠清理运行状态。

### 会话中止与稳定性

- 运行中的回复优先使用 Claude SDK 的会话内中止能力，停止当前轮次时保留会话和 MCP 连接。
- 升级内置 Claude Agent SDK 与 Claude Code 运行时，改善模型能力读取、会话控制和 Windows 运行稳定性。

## 开发者说明

- `@anthropic-ai/claude-agent-sdk` 从 `0.3.220` 升级到 `0.3.226`，内置 Claude Code 从 `2.1.220` 升级到 `2.1.226`。
- SDK Query 适配层新增 `interrupt()`、`getContextUsage()`、`supportedModels()`、`setModel()` 与 `applyFlagSettings()` 控制入口。
- 主进程新增运行时模型能力、上下文占用、模型与 effort 热切换 IPC；`effort` 纳入预启动、常驻会话指纹及发送链路。
- 上下文占用快照写入单条会话正文，但不更新会话 `updatedAt`，避免仅查看历史对话就改变排序。
- 定时任务存储增加严格读取模式、结构化错误码、启动确认回调、运行中状态和 `finally` 清理。
- 附件渲染新增基于扩展名的离线类型映射、SVG 图标和图片缩略图回退逻辑。
- 主要变更文件：`claude-sdk.js`、`main.js`、`preload.js`、`scheduler.js`、`renderer/app.js`、`renderer/index.html`、`renderer/styles.css`、`package.json`、`package-lock.json`。

## 建议回归项

1. 在已有对话中切换模型和五档推理强度，确认下一轮继续复用会话。
2. 打开长历史对话，确认上下文占用立即显示并在后台更新；重启后再次确认缓存可恢复。
3. 拖入图片、Office、PDF、代码与压缩文件，确认图标、缩略图和删除按钮正确。
4. 立即运行定时任务，确认快速返回“已开始”、显示运行中且不能重复启动。
5. 生成过程中点击停止，确认当前轮结束后历史内容仍可保存，后续消息可继续发送。
